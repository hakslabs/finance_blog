import { RoutePlaceholder } from "../RoutePlaceholder";

export function AdminPage() {
  return (
    <RoutePlaceholder
      eyebrow="Admin"
      title="관리자"
      description="수집 상태, 데이터 점검, 운영용 테이블을 둘 관리자 화면입니다."
    />
  );
}
