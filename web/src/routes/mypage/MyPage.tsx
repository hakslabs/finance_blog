import { RoutePlaceholder } from "../RoutePlaceholder";

export function MyPage() {
  return (
    <RoutePlaceholder
      eyebrow="My Page"
      title="마이페이지"
      description="사용자 설정, 관심 흐름, 계정 정보를 관리할 화면입니다."
    />
  );
}
