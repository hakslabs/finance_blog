# API deploy template.
# Local development uses the repository root `.env` copied from `../.env.example`.
# This file exists for API-only hosts that expect an app-local env template.

APP_ENV=local
LOG_LEVEL=info
# Comma-separated list. Override per environment.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
